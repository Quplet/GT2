public class Average {
    public void main(String args) {
        system.out.println("Average: " + (Integer.parseInt(args[0]) + Integer.parseInt(args[1]))/2);
	//Missing quotation marks around Average: to make it a string
	//Capitalize S in System.out.print
	//Add a semicolon after the line of code
    }
}
